import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  monkey: {
    width: 50,
    height: 100,
    position: "absolute",
  },

  branch: {
    position: "absolute",
    width: 35,
    height: 5,
  },
});

export default styles;
