import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  monkey: {
    width: 40,
    height: 80,
    position: "absolute",
  },

  branch: {
    position: "absolute",
    width: 50,
    height: 15,
  },
});

export default styles;
