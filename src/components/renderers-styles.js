import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  monkey: {
    width: 50,
    height: 100,
    position: "absolute",
  },

  branch: {
    position: "absolute",
    width: 50,
    height: 15,
  },
});

export default styles;
